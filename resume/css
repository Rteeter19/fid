 

```css
/* Reset some default styles */
body, h1, h2, h3, ul, p, li {
  margin: 0;
  padding: 0;
}

/* Set a background color and font family */
body {
  background-color: #f4f4f4;
  font-family: 'Ibarra Real Nova', serif;
}

/* Header styles */
header {
  background-color: #333;
  color: #fff;
  text-align: center;
  padding: 20px 0;
}

/* Main content container */
.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 20px;
}

/* Section styles */
.section {
  margin-bottom: 30px;
  padding: 20px;
  background-color: #fff;
  box-shadow: 0px 0px 5px rgba(0, 0, 0, 0.2);
}

.section h2 {
  color: #333;
  margin-bottom: 10px;
}

/* List styles */
ul {
  list-style: none;
  padding-left: 20px;
}

/* Link styles */
a {
  color: #007bff;
  text-decoration: none;
}

a:hover {
  text-decoration: underline;
}

/* Image styles */
img {
  display: block;
  margin: 10px auto;
}

/* Responsive styles */
@media (max-width: 768px) {
  .section {
    margin: 10px 0;
  }
}
```

You can save this code as "Resumestyles.css" and link it to your HTML document using the following line in the `<head>` section of your HTML:

```html
<link rel="stylesheet" href="Resumestyles.css">
```

Make sure both your HTML file and the CSS file are in the same directory for the link to work properly. Feel free to further customize the styles according to your preferences.
